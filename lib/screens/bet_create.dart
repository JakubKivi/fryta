import 'package:flutter/material.dart';


class BetCreate extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Bet Create'),
      ),
      body: const Center(
        child: Text('Here you can create bet'),
      ),
    );
  }
}